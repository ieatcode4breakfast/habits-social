# Legacy V1 API (Deprecated)

This directory contains the legacy V1 API. These endpoints are no longer active and have been replaced by the V2 API promoted to the parent `server/api/` directory.

## Status: Offline
This directory is prefixed with an underscore (`_v1/`), which tells Nitro to ignore it during route generation. These routes are effectively archived.
